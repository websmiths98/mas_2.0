import NetworkClient from "./NetworkClient";

export const metadata = {
  title: "Global Network | MAS Logistics",
  description: "Connected across global trade routes with our international logistics network.",
};

export default function NetworkPage() {
  return <NetworkClient />;
}